# Agent runtime + terminal slice — FINAL SANITIZED EVIDENCE BUNDLE

Prepared 2026-09-11. Status: **unmerged, non-production.** Every value below was
re-derived by command in the same session that produced this file; nothing is
carried over from memory or from a prior evidence revision.

---

## 0. Provenance incident — read first

Between the previous evidence revision and this one, the **local** git metadata
in this sandbox was rolled back to the fresh-clone state (`HEAD` at `5640a84`,
reflog containing only `clone` + `checkout`, all remote-tracking refs except
`origin/main` gone, `node_modules` and the local Node-headers shim deleted).
The working-tree files survived.

Verified before doing anything else:

| Check | Result |
|---|---|
| Remote tip of `arena/01a091cd-coderxp` | `f346ee6b8b221bb20cfa768bdb992c75cfa67b32` — intact |
| Local objects for `2e39b88`, `f346ee6`, `4f84639` | absent until `git fetch` |
| Working tree vs `f346ee6` tree, file-by-file `git hash-object` | **296 files, 0 missing, 0 mismatch** |

**No work was lost.** Recovery used `git fetch` (read-only), `git update-ref`
to restore the branch pointer to the already-pushed tip, and `git read-tree
HEAD` to resync the index. No `reset`, `clean`, discard, blanket staging, or
force-push; the working tree was never written to. Post-recovery: `HEAD` =
`f346ee6`, `git status --porcelain` empty.

The toolchain was rebuilt from scratch for these results: `npm ci` with
`npm_config_nodedir` pointed at a shim over `/usr/local/include/node` (v22.22.3
headers), because `node-gyp` cannot reach `nodejs.org` here. `node-pty` rebuilt
from source; `node_modules/node-pty/build/Release/pty.node` present.

---

## 1. Identity

| Role | SHA |
|---|---|
| Base (slice starts here) | `4f8463953fb01a3e9eb7e4694e35f071b1f05725` |
| Implementation | `2e39b88b3844fac9444a403a291dd6ccc0fc1f06` |
| Evidence (previous revision) | `f346ee6b8b221bb20cfa768bdb992c75cfa67b32` |
| Evidence (this bundle) | see commit containing this file |
| Branch | `arena/01a091cd-coderxp` |
| Iteration runtime | **node v22.22.3 — iteration evidence only** |

PR #1, re-verified this session:

| Field | Value |
|---|---|
| Number / state | 1 / **OPEN** (unmerged) |
| Base / head | `main` `5640a84…` / `feature/auth-hardening` `0d851e96718593732d5c2cd06e72657a09220211` |
| `mergeable` | `MERGEABLE` |
| `changedFiles` | **9** (unchanged) |

Patch artifact `review/pr-1/pr-1-auth-hardening-5640a84…-to-0d851e9….patch`
sha256 `be0d842bed91b2468f3073a45c505613421c6c0ad5181a82375a27abe9284c4f`,
regenerated byte-identical from `git diff 5640a84..0d851e9`, and verified to
apply cleanly onto base `5640a84` (`git apply --check`, exit 0).

Mixed tips `c3639b1` and `4f84639` are **not** authentication-only candidates
and were not treated as such. Nothing was added to PR #1.

---

## 2. Complete changed-file list

Implementation commit `2e39b88` (`git diff --numstat 4f84639..2e39b88`):

```
 529    0  app/workspace/components/AgentTerminalPanel.tsx     (new)
  25    1  app/workspace/components/RuntimePanel.tsx
 106    0  docs/node24-acquisition.md                          (new)
  73    2  lib/server/agent-runtime/authorized-provider.ts
  16   12  lib/server/agent-runtime/handlers.ts
 350    0  lib/workspace/agent-runtime-client.ts               (new)
   1    1  package.json
   3    1  scripts/smoke-agent-runtime-live.ts
   4    2  scripts/test-agent-runtime-authz.ts
 462    0  scripts/test-agent-runtime-binding.ts               (new)
 220    0  scripts/test-agent-terminal-client.ts               (new)
```

Evidence commit `f346ee6` (`git diff --numstat 2e39b88..f346ee6`):

```
   2    0  review/runtime-terminal-slice/SHA256SUMS
 206    0  review/runtime-terminal-slice/slice-evidence.md
  41    0  review/runtime-terminal-slice/smoke-transcript-20260911T1939Z.log
```

Full slice `4f84639..f346ee6`: **14 files changed, 2038 insertions, 19 deletions.**

This revision (render verification, committed on top of `9fb6848`):

```
 155    0  scripts/test-agent-terminal-render.ts               (new)
   1    1  package.json        (wires the render suite into npm test)
  42   12  review/runtime-terminal-slice/final-evidence-bundle.md
```

`app/layout.tsx` is **not** in this list and has not been modified at any point
in the slice — verified by `git diff --name-only 5640a84 HEAD -- app/layout.tsx`
returning empty.

---

## 3. Every test command and exit code

All re-run this session on the recovered tree at `f346ee6`, node v22.22.3.

| # | Command | Exit |
|---|---|---|
| 1 | `npx tsc --noEmit` | **0** |
| 2 | `npx eslint` on the nine slice files | **0** (zero output lines) |
| 3 | `npm test` (full aggregate chain) | **0** — 936 `[PASS]` lines |
| 4 | `npx tsx scripts/smoke-agent-runtime-live.ts` | **0** — `pass=32 fail=0` |
| 5 | `npx tsx scripts/test-agent-terminal-render.ts` | **0** — 5/5 sections |
| 6 | `npm run build` (unmodified) | **1** — see §9 |
| 7 | `npm ci` (rebuild after rollback) | 0 — `node-pty` rebuilt from source |

The nine linted files: `lib/workspace/agent-runtime-client.ts`,
`app/workspace/components/AgentTerminalPanel.tsx`,
`app/workspace/components/RuntimePanel.tsx`,
`lib/server/agent-runtime/authorized-provider.ts`,
`lib/server/agent-runtime/handlers.ts`,
`scripts/test-agent-runtime-binding.ts`,
`scripts/test-agent-terminal-client.ts`,
`scripts/test-agent-runtime-authz.ts`,
`scripts/smoke-agent-runtime-live.ts`.

Per-suite section counts, each run individually after the changes:

| Suite | `[PASS]` lines |
|---|---|
| `test-agent-runtime-binding.ts` (new) | 5 |
| `test-agent-terminal-client.ts` (new) | 5 |
| `test-agent-terminal-render.ts` (new) | 5 |
| `test-agent-runtime-authz.ts` | 12 |
| `test-action-authorization.ts` | 17 |
| `test-sanitizer-bytes.ts` | 6 |

Both new suites are wired into the `npm test` chain as their own steps; the
whole chain was executed end-to-end, not only the touched suites. Terminal
summaries observed in the aggregate run: `ALL AGENT RUNTIME AUTHORIZATION
TESTS PASSED`, `ALL AGENT RUNTIME REQUEST-BINDING TESTS PASSED`, `ALL AGENT
TERMINAL CLIENT TESTS PASSED`, `ALL SANITIZER BYTE TESTS PASSED`, `ALL ACTION
AUTHORIZATION TESTS PASSED`.

---

## 4. Live transcript — exact path

```
review/runtime-terminal-slice/smoke-transcript-20260911T2029Z.log
```

41 lines, 4480 bytes, `pass=32 fail=0`, exit 0. Produced by the real `node-pty`
factory exclusively (no stubs on the smoke path). Runtime and prerequisites are
recorded inside the transcript itself:

```
[2026-09-11T20:29:58.857Z] LIVE SMOKE START — node v22.22.3 (iteration runtime; pinned-Node-24 evidence is separate)
[2026-09-11T20:29:58.857Z] platform=linux uid=1001 userns/netns/prlimit required by config
...
[2026-09-11T20:30:12.639Z] LIVE SMOKE END — pass=32 fail=0
```

Environment prerequisites exercised: linux, non-root uid 1001, user+network
namespace (`unshare -U -n -r`), `prlimit`, native `node-pty`.

**Sanitization:** scanned for secret assignments, bearer tokens, password
literals, and JWT-shaped strings — zero matches. The transcript carries only
synthetic test values; lease tokens and approval serializations are never
printed.

The earlier revision's transcript
(`smoke-transcript-20260911T1939Z.log`, also 32/32) is retained unchanged for
continuity.

---

## 5. Isolation and workspace-persistence results

Isolation (verbatim from the transcript):

```
[20:29:58.927Z] sessA isolation={"user":"user","netns":"no-egress","prlimit":true} shellPid=4216
[20:29:58.971Z] [CHECK pass] userns root-mapped inside (unprivileged outside)
[20:29:58.977Z] [CHECK pass] files owned by unprivileged host uid — uid=1001
[20:29:58.977Z] [CHECK pass] no egress route inside netns — default route absent
[20:29:58.977Z] [CHECK pass] session env carries identity, no host secrets — scrubbed
[20:30:03.006Z] [CHECK pass] sessA cannot see sessB files
[20:30:03.007Z] [CHECK pass] workspace escape refused — MALFORMED_REQUEST
[20:30:03.007Z] [CHECK pass] userB denied on userA session
```

Workspace persistence across runtime replacement:

```
[20:30:04.211Z] [CHECK pass] persisted workspaces listed after replacement — ["sessA:2","sessB:1"]
[20:30:04.211Z] [CHECK pass] sessA file content survives replacement
[20:30:04.221Z] [CHECK pass] project ownership survives replacement
[20:30:04.226Z] [CHECK pass] new session allocatable after replacement
```

Lost-supervisor honesty (no fabricated state):

```
[CHECK pass] killed session reports dead (not running) — shell exited
[CHECK pass] exec on dead session refused — RUNTIME_UNAVAILABLE
[CHECK pass] replacement runtime reports old session unknown
```

---

## 6. Reconnect / no-rerun proof

```
[20:30:02.832Z] [CHECK pass] replay reconciled without rerun — lines=1
```

The repeated execution request was reconciled by operation ID against the op
ledger; the side effect (a line appended in the workspace) occurred exactly
once — `lines=1`, not 2. The outcome carries `reconciled: true` rather than a
second execution. The client-side counterpart (server-issued cursor echoed
back, never a client-invented one) is covered by
`test-agent-terminal-client.ts` §3, which asserts the resume URL contains
`cursor=7` and that no frame is redelivered after dispose.

---

## 7. Cancellation, timeout, and descendant termination

```
[20:30:01.490Z] [CHECK pass] timeout kills with confirmation —
  {"kind":"timeout","exitCode":null,"signal":"SIGTERM","confirmed":true,
   "reconciled":false,"outputBytes":0,"outputTruncated":false,"durationMs":2505}
[20:30:02.022Z] [CHECK pass] no descendant sleep survives — pgrep=""
[20:30:02.823Z] [CHECK pass] cancel accepted
[20:30:02.824Z] [CHECK pass] cancelled outcome —
  {"kind":"cancelled","exitCode":null,"signal":"SIGTERM","confirmed":true,
   "reconciled":false,"outputBytes":0,"outputTruncated":false,"durationMs":801}
```

Note the honesty fields: `exitCode` is `null` (never coerced to 0), and the
descendant check is a real `pgrep` returning empty — termination was verified
against the whole isolated workload, not assumed.

Redaction:

```
[20:29:58.983Z] [CHECK pass] live token redacted from stream — marker present
```

Split-across-chunks secret redaction is covered deterministically by
`test-sanitizer-bytes.ts` §3–4 (`Split synthetic secrets withheld then
redacted`, `Base patterns, prefix withholding, and synthetics compose`) and by
`test-agent-runtime-authz.ts` §9 (`Chunk-split secrets redacted at the
buffer`), both green in the aggregate run.

---

## 8. The two corrected stale-approval checks

Binding the exec timeout into the authorized `args` invalidated two existing
checks that issued exec approvals without it. In both cases the provider was
**correctly** never called; the fix was to bind the timeout in the approval,
not to weaken the assertion.

| Check | Before fix | After fix |
|---|---|---|
| `test-agent-runtime-authz.ts` §5 "Shell-script is approval-gated at the HTTP boundary" | `Error: timed out waiting for shell-script child spawn`, suite exit 1 | `[PASS] Grants never cover shell-script; exact approvals do.` — 12/12, exit 0 |
| `smoke-agent-runtime-live.ts` "shell-script via exact approval runs" | `pass=31 fail=1`, `[CHECK FAIL]`, exit 1 | `[CHECK pass]`, `pass=32 fail=0`, exit 0 |

Binding regressions are now pinned by `test-agent-runtime-binding.ts`, which
mutates each bound field individually against one approval and asserts
`APPROVAL_MISMATCH` **and** `childSpawns() === 0` for every mutation: argv
element, argv length, cwd (`resource`), `networkNeed`, `execMode`, and
`timeoutMs`.

---

## 9. Explicitly unrun / blocked

1. **`npm run build` — exit 1. Not worked around, not hidden.** Exactly two
   errors, both `next/font/google`:
   `Failed to fetch Inter from Google Fonts` and
   `Failed to fetch JetBrains Mono from Google Fonts`.
   The source is **`app/layout.tsx`, unmodified from base** — verified by
   `git diff --name-only 5640a84 HEAD -- app/layout.tsx` returning empty. The
   importing statement is `app/layout.tsx:2: import { Inter, JetBrains_Mono }
   from "next/font/google";`. Root cause in this sandbox is network, not code:
   `fonts.googleapis.com` resolves but the TLS handshake fails (`curl` exit 35,
   HTTP 000). Switching to `next/font/local` would change the artifact under
   test, so it was not done. Full production-build evidence remains owed on a
   supported runtime with font egress.
2. **Node 24 verification — not run.** No Node 24 artifact was downloaded,
   installed, or executed. `nodejs.org` is unreachable here (`curl` exit 35,
   HTTP 000). The pinned **v24.21.0 linux-x64** artifact, source URL, and the
   `SHASUMS256.txt` + `SHASUMS256.txt.asc` GPG verification chain in
   `docs/node24-acquisition.md` remain **unverified acquisition inputs**; the
   sha256 recorded there was transcribed from the official listing, not
   computed in-sandbox, and the document says so. No network-policy exception
   was granted, assumed, or used. Every result above is Node v22.22.3
   iteration evidence only.
3. **Browser rendering of `AgentTerminalPanel` — partially closed, still not
   browser-verified.** The dev server cannot compile `app/layout.tsx` for the
   same font reason, so the panel has not rendered in a browser.

   What is now verified beyond typecheck: `scripts/test-agent-terminal-render.ts`
   bundles the real component tree with esbuild and renders `RuntimePanel`
   (including `AgentTerminalPanel`) to static markup via `react-dom/server`.
   This required **no change to `app/layout.tsx`** — it bypasses Next.js
   routing entirely, so the build-blocker evidence in item 1 is untouched and
   no font substitution was performed. Assertions, scoped to the agent pane so
   the PORTS pane's legitimate "RUNNING" pill cannot satisfy a check:
   - AGENT renders as its own pane and tab alongside the user TERMINAL
   - initial state reads `NO SESSION` / `conn: idle`, and contains none of
     `RUNNING`, `DEAD`, or `UNKNOWN` — no fabricated state
   - observe-only by default, with the keystroke-refusal notice, plus
     Take input / Release input
   - Allocate, Reconnect, Resize, Interrupt, Stop, Probe all render
   - exactly 7 session-scoped controls disabled, with Allocate the only
     enabled one
   - terminal container carries `aria-label="Agent session terminal"`

   Still **unverified**: real browser layout, xterm canvas behaviour, and live
   network streaming in a browser. SSR runs no effects, so the xterm instance
   is never constructed and no fetch is issued. Those require the build to
   complete.
4. **Live smoke is environment-dependent and is not aggregate-suite evidence.**
   It requires linux + non-root uid + user/network namespaces + `prlimit` +
   native `node-pty`. It is deliberately **not** part of `npm test`. Its
   transcript, runtime version (v22.22.3), base SHA (`4f84639`),
   implementation SHA (`2e39b88`), prerequisites, and exit code (0) are
   recorded in §4.

---

## 10. Boundaries respected

Slice remains **unmerged and non-production**. PR #1 untouched at `0d851e9`
(still OPEN, 9 files). No merge, no Actions enablement, no paid
infrastructure, no production contact or deployment, no live-secret rotation.
No claim is made of Node 24, production-build, or browser verification.
Git-host, deployment, and ZIP adapters remain separate gated follow-up slices.
Only `arena/01a091cd-coderxp` was pushed; `main`, `feature/auth-hardening`,
`arena/01a09099-coderxp`, and `review/pr-1-artifacts-0d851e9` are unchanged.
